<%= form_with(model: work_breakdown_structure) do |form| %>
  <% if work_breakdown_structure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(work_breakdown_structure.errors.count, "error") %> prohibited this work_breakdown_structure from being saved:</h2>

      <ul>
        <% work_breakdown_structure.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
